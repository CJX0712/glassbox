"""The built-in demo corpus.

Glassbox must be useful the moment it starts, with no downloads of your own
documents.  These twelve notes cover the retrieval and modelling ideas the
engine itself is built from, which makes the demo self-referential in a useful
way: you can ask *the engine* how the engine works and watch the trace.

They are deliberately bilingual.  Chinese and English sit in the same
paragraphs so both halves of the hybrid retriever have something to win on:
Chinese queries lean on the BM25 bigram tokenizer, paraphrases lean on the
dense embeddings.
"""

from __future__ import annotations

SAMPLE_DOCS: list[tuple[str, str, str]] = [
    (
        "01-rag",
        "检索增强生成 (RAG)",
        "检索增强生成（Retrieval-Augmented Generation, RAG）把外部知识库的检索结果作为上下文喂给语言模型，"
        "让模型在不重新训练的前提下回答关于私有数据的问题。\n\n"
        "RAG 的基本流程是：先把文档切成块并建立索引；用户提问时先检索出最相关的若干片段；"
        "再把片段与问题一起拼成提示词交给模型生成答案。这样做的价值有三点：知识可以随时更新，"
        "不必重训模型；答案可以标注出处，便于核查；幻觉被上下文约束，显著下降。\n\n"
        "工程上 RAG 的瓶颈几乎总在检索而不是生成。检索召回不到正确片段，再强的模型也无能为力，"
        "这就是为什么生产系统普遍采用混合检索加交叉编码器重排的两段式结构。",
    ),
    (
        "02-bm25",
        "BM25 稀疏检索",
        "BM25（Okapi BM25）是词袋检索里最经久不衰的排序函数。它对每个查询词计算逆文档频率 IDF，"
        "再乘上一个饱和的项频因子：随着某个词在文档中出现次数增加，得分增长会逐渐放缓，"
        "这就是所谓的 term frequency saturation。参数 b 用来做文档长度归一化，长文档不会因为字数多而占便宜。\n\n"
        "BM25 最强的地方是精确匹配：产品型号、错误码、人名、年份这类字符串，稠密向量往往把它们平滑掉，"
        "而 BM25 能够逐字命中。它的弱点是同义改写，用户换一种说法，词就对不上了。\n\n"
        "对中文来说，BM25 需要一个合适的分词器。按空格切分毫无意义，"
        "常见的轻量做法是把连续汉字切成字符二元组（bigram），例如「玻璃盒」变成 玻 / 玻璃 / 璃盒 / 盒，"
        "在几乎零成本的前提下恢复大部分检索信号。",
    ),
    (
        "03-dense",
        "稠密向量检索 (Dense Retrieval)",
        "稠密检索把查询和段落映射到同一个向量空间，用余弦相似度衡量语义接近程度。"
        "因为编码的是语义而不是字面，稠密检索天然能处理同义改写和跨语言查询。\n\n"
        "实现上通常分两步：离线用编码器把全部段落编码成矩阵并建索引（FAISS、HNSW 等），"
        "在线只编码查询，然后做一次近似或精确的内积搜索。把向量做 L2 归一化之后，"
        "余弦相似度恰好等于内积，于是整条检索链路退化为一次矩阵乘法，这也是工程上最常见的做法。\n\n"
        "稠密检索的短板与 BM25 正好互补：罕见专有名词和长数字在向量里被压缩掉，"
        "因此纯向量检索在需要精确匹配时表现疲软。把稀疏和稠密两路结果融合，"
        "是当前混合检索的标准配方。",
    ),
    (
        "04-rrf",
        "倒数排名融合 (Reciprocal Rank Fusion)",
        "倒数排名融合（Reciprocal Rank Fusion, RRF）把多个排好序的列表合成一个。"
        "每个文档的得分等于它在每个检索器里的 1/(k + rank) 之和，常数 k 通常取 60。\n\n"
        "为什么不用加权分数相加？因为 BM25 的分数没有上界且依赖语料规模，"
        "而余弦相似度落在 -1 到 1 之间，两者量纲根本不可比。任何加权求和都需要逐查询校准权重，"
        "而 RRF 只消费排名，天然免疫分数尺度问题，这正是它成为混合检索默认选择的原因。\n\n"
        "常数 k 的作用是压制最靠前几位的影响：k 越大，排名 1 和排名 2 的差距越小，"
        "单个检索器就越难独断结果。这使得融合结果对某一个检索器的偶发失误更鲁棒。",
    ),
    (
        "05-rerank",
        "交叉编码器重排 (Cross-encoder Reranking)",
        "第一段检索是快的、近似的。无论是 BM25 还是双塔向量模型，查询和段落都是各自独立编码的，"
        "彼此从未见过面。交叉编码器（cross-encoder）不同：它把查询和一段候选文本拼在一起送进 Transformer，"
        "输出一个相关性分数。这种联合编码的精度远高于比较两个独立向量。\n\n"
        "代价是速度：交叉编码器无法预先计算，每个查询与每个候选都要跑一次前向，"
        "因此只能作用在一个很小的候选集上。这就是「先检索、后重排」两段式结构存在的原因——"
        "用廉价的第一段把上千个候选砍到几十个，再用昂贵的重排器精挑细选。\n\n"
        "实测中重排往往能显著提升 top-3 命中率，因为它修正了向量模型对细节的迟钝。"
        "如果重排器不可用，系统应当优雅降级为直接使用融合排序，而不是报错。",
    ),
    (
        "06-chunking",
        "分块策略 (Chunking)",
        "分块是 RAG 里最容易被低估的一步。按固定字符数硬切会把句子拦腰截断，"
        "而答案往往恰好就在那个断点上。句子感知的分块会先把文本切成完整句子，"
        "再按窗口大小装填，并保留一段重叠（overlap）。重叠保证了跨边界的事实至少完整地出现在某一块里。\n\n"
        "块大小是一对权衡：小块定位精确、噪声少，但缺少上下文；大块上下文充分，"
        "但会把无关内容一起塞进提示词，既浪费预算也稀释注意力。经验上 500 到 1000 字符是常见区间。\n\n"
        "一个可验证的性质是：好的分块不丢句子。把全部块的文本拼起来，"
        "原文的每一句都应当能在其中找到，这可以作为分块器的自检不变量。",
    ),
    (
        "07-embeddings",
        "文本嵌入 (Text Embeddings)",
        "文本嵌入把一段文字映射成一个固定长度的稠密向量，语义相近的文本在向量空间里彼此靠近。"
        "现代嵌入模型大多基于 Transformer，用对比学习在海量句对上训练，"
        "让正样本对的距离拉近、负样本对的推远。\n\n"
        "使用嵌入模型有几个实践细节。第一，很多模型对查询和段落使用不同的前缀"
        "（例如 E5 系列的 query: 与 passage:），漏掉前缀会明显掉点。"
        "第二，不同模型不能混用，索引和查询必须来自同一个模型。"
        "第三，输出向量通常要做 L2 归一化，让余弦相似度等价于内积，便于用矩阵乘法批量计算。\n\n"
        "小模型（几十到一两百 MB）在 CPU 上也能跑得很快，配合 ONNX Runtime 甚至不需要 PyTorch，"
        "这对内存紧张的机器非常关键。",
    ),
    (
        "08-quantization",
        "模型量化 (Quantization)",
        "量化把模型的权重从高精度浮点压到低位宽整数，换取更小的体积和更快的内存受限推理。"
        "最常见的格式是 4 位量化，即把每个权重压到 4 bit，体积降到 FP16 的四分之一左右。\n\n"
        "对本地部署来说，量化的意义不只是省磁盘：权重越小，内存带宽压力越低，"
        "而 CPU 推理的瓶颈往往正是内存带宽。一个 5 亿参数的模型在 4 位量化后大约 400 MB，"
        "可以在 16 GB 内存的笔记本上与嵌入模型、重排器共存。\n\n"
        "代价是精度损失。通常 8 位几乎无损，4 位在大多数任务上仍有很好表现，"
        "但极低比特（2 到 3 位）会开始明显退化，尤其是需要精细推理的任务。"
        "量化是让世界级模型跑在消费级硬件上的关键一环。",
    ),
    (
        "09-attention",
        "注意力机制 (Self-Attention)",
        "自注意力让序列中的每个位置都能直接看到其他所有位置。"
        "输入被投影成查询 Q、键 K、值 V 三组向量，注意力权重由 Q 与 K 的点积经过缩放和 softmax 得到，"
        "再对 V 加权求和。缩放因子是维度的平方根，用来避免点积随维度增大而方差爆炸、softmax 趋于饱和。\n\n"
        "解码器里的注意力需要因果掩码：位置 i 只能关注位置 j 小于等于 i 的内容，"
        "否则模型在预测下一个词时会偷看答案。实现上通常把未来位置的注意力分数设为一个极大的负数，"
        "softmax 之后它们的权重精确为 0。\n\n"
        "多头注意力把表示切成若干个子空间并行做注意力，再拼接起来，"
        "使模型能同时关注不同类型的依赖关系。注意力矩阵本身可解释性有限，"
        "但把它画出来仍然有助于调试模型到底在看哪里。",
    ),
    (
        "10-eval",
        "检索评测指标 (Recall, MRR, nDCG)",
        "评测是检索系统开发的指南针。最基础的指标是 Recall@k：前 k 个结果里是否包含正确答案，"
        "它衡量检索器的上限——排在 k 名之外的内容，后面的重排器再强也救不回来。\n\n"
        "MRR（平均倒数排名）关注第一个正确结果出现的位置，对排序前段特别敏感。"
        "nDCG 则引入了分级相关性，并用对数折扣压低靠后位置的影响，"
        "是排序质量最全面的单指标之一。\n\n"
        "实践中要区分检索评测和端到端评测。检索指标只看有没有召回正确片段，"
        "而 RAG 的最终质量还取决于生成是否忠实于上下文。"
        "把两者分开度量，才能知道问题出在召回、重排还是生成。",
    ),
    (
        "11-transformers",
        "Transformer 架构",
        "Transformer 抛弃了循环结构，完全依靠注意力在序列内传递信息。"
        "一个标准块由多头自注意力和前馈网络组成，两者之间用残差连接和层归一化串联。"
        "前馈网络对每个位置独立作用，通常先把维度放大四倍再压回，提供非线性变换能力。\n\n"
        "层归一化的位置有两种主流选择：后置（Post-LN，原始论文）和前置（Pre-LN）。"
        "Pre-LN 把归一化放在子层之前，训练更稳定，不需要精心设计的学习率预热，"
        "因此成为现代大模型的默认选择。\n\n"
        "位置信息在注意力机制里天然缺失，必须显式注入。"
        "正弦位置编码用不同频率的三角函数为每个位置生成唯一向量，"
        "旋转位置编码（RoPE）则把位置信息编码成向量的旋转，二者都能让模型感知次序。",
    ),
    (
        "12-agents",
        "智能体与自我纠错 (Agentic Retrieval)",
        "朴素 RAG 是单向的：检索一次，生成一次。智能体式检索把它变成一个闭环——"
        "检索之后先评估证据是否足够，不够就改写查询再来一轮。\n\n"
        "这个评估步骤叫作相关性打分（relevance grading）。"
        "一个透明且不依赖大模型的信号是词项覆盖率：把查询拆成实词，统计它们在检索结果中出现的比例。"
        "覆盖率过低说明检索跑偏了，应当改写而不是硬答。\n\n"
        "改写查询也有两条路。有大模型时可以让模型重写，保留实体、补充同义词、去掉冗余；"
        "没有大模型时可以用伪相关反馈（pseudo-relevance feedback）："
        "从当前排在最前的段落里提取高频且查询中没有出现的词，追加到查询上。"
        "后者是信息检索里的经典技巧，零成本且完全离线可用。\n\n"
        "闭环的价值在于容错：单次检索的失误不会直接变成错误答案，"
        "系统还有机会在第二轮把正确片段找回来。",
    ),
]


def triples() -> list[tuple[str, str, str]]:
    return list(SAMPLE_DOCS)
